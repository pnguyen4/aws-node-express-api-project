{
  "name": "aws-node-express-api-project",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "npx tsc --outDir dist",
    "dev": "concurrently \"npx tsc --watch --outDir dist\" \"nodemon dist/server.js\" # spins up local instance on dev machine",
    "deploy": "serverless deploy   # pushes latest code to cloud"
  },
  "dependencies": {
    "@apollo/server": "^4.3.2",
    "@types/express": "^4.17.16",
    "@types/node": "^18.11.18",
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.17.1",
    "graphql-tag": "^2.12.6",
    "serverless-http": "^2.7.0",
    "typescript": "^4.9.5"
  },
  "devDependencies": {
    "concurrently": "^7.6.0",
    "serverless-offline": "^12.0.4"
  }
}
